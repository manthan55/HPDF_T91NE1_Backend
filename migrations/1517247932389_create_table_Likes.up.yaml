- args:
    sql: CREATE TABLE "Likes"("like_user_id" integer NOT NULL, "likeby_user_id" integer
      NOT NULL, PRIMARY KEY ("like_user_id") )
  type: run_sql
- args:
    name: Likes
  type: add_existing_table_or_view
